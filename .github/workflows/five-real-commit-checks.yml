name: Five Real Commit Checks

on:
  push:
    branches: [battle-5check-real]

permissions:
  contents: read

jobs:
  commit-check:
    name: Commit ${{ matrix.slot }} - Strategic controls
    runs-on: ubuntu-latest
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        slot: [1, 2, 3, 4, 5]

    steps:
      - name: Checkout history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve exact commit
        id: target
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t COMMITS < <(git rev-list --max-count=5 --reverse "${{ github.sha }}")
          test "${#COMMITS[@]}" -eq 5
          SLOT="${{ matrix.slot }}"
          TARGET="${COMMITS[$((SLOT - 1))]}"
          echo "sha=$TARGET" >> "$GITHUB_OUTPUT"
          echo "Checking commit $SLOT: $TARGET"

      - name: Install Godot 4.7.2
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL -o /tmp/godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          sudo install -m 0755 /tmp/godot/Godot_v4.7.2-stable_linux.x86_64 /usr/local/bin/godot
          godot --version | grep -q '^4\.7\.2'

      - name: Checkout exact commit
        shell: bash
        run: |
          set -euo pipefail
          git checkout --detach "${{ steps.target.outputs.sha }}"
          git show -s --format='Checking %H %s'

      - name: Run real strategic control smoke
        shell: bash
        run: |
          set -euo pipefail
          timeout 30s godot --headless --path . --script source/world/UnitStateSmoke.gd
